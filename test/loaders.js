import loaders from '../src/js/loaders';

var assert = require('assert');

describe('Моча', function () {
    var num = 0
    before(function () {
        num = Math.random()
        console.info("Говно")
    })

    it ("loaders", function () {
        //assert.equal(loaders.test(), 'test');
    })
});